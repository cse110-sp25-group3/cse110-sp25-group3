import { getUniqueValues } from "../../functions/fetch-jobs.js";

export async function renderPreferences(container) {
  // clear and set up title
  container.innerHTML = `<h2>Preferences</h2>`;

  
}